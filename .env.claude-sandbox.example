# Environment variables for the Claude Code sandbox container.
#
# The claude-sandbox launcher (github.com/kmacmcfarlane/claude-sandbox) passes
# this file to `docker run --env-file`, making every variable available inside
# the container. It looks for .claude-sandbox/env (walking parent directories
# from the project root) and warns if the file is missing.
#
# Setup:
#   cp .env.claude-sandbox.example .claude-sandbox/env
#   # Fill in your values, then run: make claude
#
# This file is gitignored — do not commit it.

# Discord webhook for MCP notification server
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN

# Discord webhook for Claude Code notification hooks (permission prompts, idle)
CLAUDE_NOTIFICATION_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN