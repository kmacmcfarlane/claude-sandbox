{
  "hooks": {
    "Notification": [
      {
        "matcher": "idle_prompt|permission_prompt",
        "hooks": [
          {
            "type": "command",
            "command": "[ -n \"${CLAUDE_NOTIFICATION_WEBHOOK_URL:-}\" ] && curl -s -H 'Content-Type: application/json' -d '{\"content\":\"🔔 Claude Code needs your input\"}' \"$CLAUDE_NOTIFICATION_WEBHOOK_URL\" > /dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
