# TODO — moved

Work items now live in `.claude-sandbox/work/`, one file per item, managed by
`wi` (claude-kit `skills/work-items/scripts/wi.py`).

- `wi prime` — what's next, in ≤300 tokens; start every session here
- `wi next` / `wi show <id> --brief` — pick and inspect an item
- `wi add "title"` / `wi claim` / `wi handoff` / `wi done` — the write verbs

Format and rules: claude-kit `skills/work-items/references/format.md`.

Migrated 2026-09-01 (4 items, all open). The guardrails-scaffold feature
(`docs/guardrails-scaffold/PLAN.md`) was added at migration time.
